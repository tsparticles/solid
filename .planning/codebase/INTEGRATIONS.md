# External Integrations

**Analysis Date:** 2026-04-10

## APIs & External Services

**Package Registry & Distribution:**
- npm registry - Distribution target for `@tsparticles/solid` package.
  - SDK/Client: npm/pnpm publish pipeline via `components/solid/package.json` (`publishConfig.access: public`, `prepublishOnly`).
  - Auth: Not configured in repository files; handled by publisher environment outside tracked code.

**Remote Particle Configuration (consumer-provided):**
- Arbitrary JSON endpoint - Component accepts remote config URL via prop and delegates loading to tsParticles.
  - SDK/Client: `@tsparticles/engine` client call path in `components/solid/src/Particles.tsx` (`tsParticles.load({ ..., url })`).
  - Auth: Not implemented in this codebase; any auth headers/tokens must be handled by endpoint accessibility strategy.

**Developer Platforms (project metadata/community):**
- GitHub Sponsors links in package metadata (`components/solid/package.json`) and funding file (`.github/FUNDING.yml`).
  - SDK/Client: Not applicable.
  - Auth: Not applicable.

## Data Storage

**Databases:**
- Not detected.
  - Connection: Not applicable.
  - Client: Not applicable.

**File Storage:**
- Local filesystem only for source/build artifacts (`apps/solid/dist`, `components/solid/dist` generated by build scripts).

**Caching:**
- CI dependency cache via GitHub Actions cache in `.github/workflows/nodejs.yml` (pnpm store path).

## Authentication & Identity

**Auth Provider:**
- None in runtime application/library code.
  - Implementation: Not applicable.

## Monitoring & Observability

**Error Tracking:**
- No external error tracking service detected.

**Logs:**
- Console logging only in library internals/build scripts (`components/solid/src/lib/ParticlesProvider.tsx`, `components/solid/tsup.config.ts`).

## CI/CD & Deployment

**Hosting:**
- GitHub repository-hosted source with npm package distribution target indicated by `components/solid/package.json`.
- Demo app is static-build compatible (`apps/solid/package.json` build output), with no repository-defined host binding.

**CI Pipeline:**
- GitHub Actions workflow in `.github/workflows/nodejs.yml`.
- Pipeline installs dependencies and runs `npx lerna run build:ci` across workspace packages.

## Environment Configuration

**Required env vars:**
- Runtime app/library: none required by current code paths.
- Optional/build-time flags: `CI`, `GITHUB_ACTIONS` used in `components/solid/tsup.config.ts`.
- Test env values injected by Vitest config (`NODE_ENV`, `DEV`, `SSR`, `PROD`) in `components/solid/vitest.config.ts`.

**Secrets location:**
- Repository does not include tracked secret files; `.env*` not detected at root.
- CI secret reference is commented out (`NX_CLOUD_ACCESS_TOKEN`) in `.github/workflows/nodejs.yml`.

## Webhooks & Callbacks

**Incoming:**
- None detected.

**Outgoing:**
- None from repository runtime code.
- Optional HTTP GET for remote particle options can be triggered by consumers using the `url` prop (`components/solid/src/IParticlesProps.ts`, `components/solid/src/Particles.tsx`).

---

*Integration audit: 2026-04-10*
